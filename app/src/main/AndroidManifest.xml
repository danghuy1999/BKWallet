<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nguyen.huy.moneylover">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainTruong.MainKeHoach"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".MainTruong.ThemKeHoach"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name=".MainTruong.ChonNhomActivity"
            android:theme="@style/Base.Theme.AppCompat.Light.Dialog" />
        <activity android:name=".MinhLayout.ThuChiActivity" />
        <activity android:name=".MinhLayout.ChonNhomActivity" />
    </application>

</manifest>